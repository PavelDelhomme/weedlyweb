#include "GestionnaireHTTP.h"

void GestionnaireHTTP::recuperer(const std::string& url) {
    std::cout << "Requête HTTP en cours vers " << url << std::endl;
    // Logique de récupération HTTP (curl ou autre)
}
